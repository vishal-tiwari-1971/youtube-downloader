import React from 'react';
import Form from "../components/Form";

const Ins = () => {  // Capitalize component name (e.g., 'Ins')

    <div>
      <h3 className='text-black'>Insta</h3>
      {/* </>  This will render the 'Form' component */}
    </div>
 
};

export default Ins;  // Export the 'Ins' component (capitalized)
